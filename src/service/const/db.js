'use strict';

const CategorySortType = {
  BY_NAME: `name`,
  BY_POST_COUNT: `count`,
};

const PostSortType = {
  BY_DATE: `date`,
  BY_POPULARITY: `popularity`,
};

module.exports = {
  CategorySortType,
  PostSortType,
};
